# Klip Dashboard

开发环境（Windows PowerShell）：

1. 进入项目目录：

```powershell
cd .\klip
```

2. 安装依赖：

```powershell
npm install
```

3. 启动开发服务器：

```powershell
npm run dev
```

项目基于 Vite + React，依赖包括 `react`, `react-dom`, `recharts`, `react-icons`。
